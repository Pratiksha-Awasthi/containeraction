name: 'my first container action'
description 'A cloud guru'
author: 'Chad crowell'
inputs:
  myInput:
    description: 'greetion to use'
    required: true
    default: 'Awesome Guru'
  outputs:
    myOutput:
      description: 'total memory'
  runs:
    using: 'docker'
    image: 'Dockerfile'
